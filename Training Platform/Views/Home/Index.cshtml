@{
    ViewData["Title"] = Localizer["HomePage"];
}

<div class="text-center">
    <h1 class="display-4">@Localizer["Welcome"]</h1>
    <p>@Html.Raw(Localizer["LearnAboutBuildingWebApps"])</p>
</div>
