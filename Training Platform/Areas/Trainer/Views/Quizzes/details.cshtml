@using Training_Platform.Models
@model Quiz

@{
    ViewData["Title"] = "Quiz Details";

    var questions = Model.Questions?
        .OrderBy(q => q.Id)
        .ToList()
        ?? new List<Question>();

    var results = Model.QuizResults?
        .OrderByDescending(r => r.SubmittedAt)
        .ToList()
        ?? new List<QuizResult>();

    var totalMarks = questions.Sum(q => q.Mark);
}

<div class="container-fluid">

    <!-- ================= HEADER ================= -->

    <div class="d-flex justify-content-between align-items-center mb-4">

        <div>
            <h2 class="fw-bold mb-1">
                <i class="bi bi-clipboard-check me-2"></i>
                Quiz Details
            </h2>

            <p class="text-muted mb-0">
                View quiz information, questions, and trainee results.
            </p>
        </div>

        <div class="d-flex gap-2">

            <a asp-action="Edit"
               asp-route-id="@Model.Id"
               class="btn btn-primary">

                <i class="bi bi-pencil me-1"></i>
                Edit

            </a>

            <a asp-action="Index"
               class="btn btn-outline-secondary">

                <i class="bi bi-arrow-left me-1"></i>
                Back to Quizzes

            </a>

        </div>

    </div>


    <!-- ================= QUIZ INFORMATION ================= -->

    <div class="card shadow-sm border-0 mb-4">

        <div class="card-header bg-white py-3">

            <h5 class="mb-0 fw-semibold">
                <i class="bi bi-info-circle me-2"></i>
                Quiz Information
            </h5>

        </div>

        <div class="card-body">

            <div class="row g-4">

                <!-- Title -->
                <div class="col-md-4">

                    <div class="text-muted small mb-1">
                        Quiz Title
                    </div>

                    <div class="fw-semibold fs-5">
                        @Model.Title
                    </div>

                </div>


                <!-- Course -->
                <div class="col-md-4">

                    <div class="text-muted small mb-1">
                        Course
                    </div>

                    <div class="fw-semibold">
                        @(Model.Course?.Title ?? "Unknown Course")
                    </div>

                </div>


                <!-- Passing Score -->
                <div class="col-md-4">

                    <div class="text-muted small mb-1">
                        Passing Score
                    </div>

                    <div class="fw-semibold">
                        @Model.PassingScore%
                    </div>

                </div>


                <!-- Time Limit -->
                <div class="col-md-4">

                    <div class="text-muted small mb-1">
                        Time Limit
                    </div>

                    <div class="fw-semibold">
                        @Model.TimeLimit minutes
                    </div>

                </div>


                <!-- Questions -->
                <div class="col-md-4">

                    <div class="text-muted small mb-1">
                        Questions
                    </div>

                    <div class="fw-semibold">
                        @questions.Count
                    </div>

                </div>


                <!-- Total Marks -->
                <div class="col-md-4">

                    <div class="text-muted small mb-1">
                        Total Marks
                    </div>

                    <div class="fw-semibold">
                        @totalMarks
                    </div>

                </div>

            </div>

        </div>

    </div>


    <!-- ================= QUESTIONS ================= -->

    <div class="card shadow-sm border-0 mb-4">

        <div class="card-header bg-white py-3">

            <div class="d-flex justify-content-between align-items-center">

                <div>

                    <h5 class="mb-1 fw-semibold">
                        <i class="bi bi-question-circle me-2"></i>
                        Questions
                    </h5>

                    <small class="text-muted">
                        Manage questions for this quiz.
                    </small>

                </div>


                <a asp-area="Trainer"
                   asp-controller="Questions"
                   asp-action="Create"
                   asp-route-quizId="@Model.Id"
                   class="btn btn-primary btn-sm">

                    <i class="bi bi-plus-lg me-1"></i>
                    Add Question

                </a>

            </div>

        </div>


        <div class="card-body p-0">

            @if (questions.Any())
            {
                <div class="table-responsive">

                    <table class="table table-hover align-middle mb-0">

                        <thead class="table-light">

                            <tr>

                                <th class="ps-4" style="width:70px;">
                                    #
                                </th>

                                <th>
                                    Question
                                </th>

                                <th style="width:100px;">
                                    Mark
                                </th>

                                <th style="width:170px;">
                                    Type
                                </th>

                                <th class="text-end pe-4" style="width:180px;">
                                    Actions
                                </th>

                            </tr>

                        </thead>

                        <tbody>

                            @{
                                int questionNumber = 1;
                            }

                            @foreach (var question in questions)
                            {
                                <tr>

                                    <td class="ps-4 fw-semibold">
                                        @questionNumber
                                    </td>

                                    <td>

                                        <div class="fw-semibold">
                                            @question.QuestionText
                                        </div>

                                    </td>

                                    <td>

                                        <span class="badge bg-light text-dark border">
                                            @question.Mark
                                        </span>

                                    </td>

                                    <td>

                                        @if (question.QuestionType ==
                                            QuestionType.MultipleChoice)
                                        {
                                            <span class="badge bg-primary-subtle text-primary">
                                                Multiple Choice
                                            </span>
                                        }
                                        else
                                        {
                                            <span class="badge bg-success-subtle text-success">
                                                True / False
                                            </span>
                                        }

                                    </td>

                                    <td class="text-end pe-4">

                                        <div class="d-inline-flex gap-1">

                                            <!-- Details -->
                                            <a asp-area="Trainer"
                                               asp-controller="Questions"
                                               asp-action="Details"
                                               asp-route-id="@question.Id"
                                               class="btn btn-sm btn-outline-info"
                                               title="View Details">

                                                <i class="bi bi-eye"></i>

                                            </a>


                                            <!-- Edit -->
                                            <a asp-area="Trainer"
                                               asp-controller="Questions"
                                               asp-action="Edit"
                                               asp-route-id="@question.Id"
                                               class="btn btn-sm btn-outline-primary"
                                               title="Edit Question">

                                                <i class="bi bi-pencil"></i>

                                            </a>


                                            <!-- Delete -->
                                            <form asp-area="Trainer"
                                                  asp-controller="Questions"
                                                  asp-action="Delete"
                                                  asp-route-id="@question.Id"
                                                  asp-route-quizId="@Model.Id"
                                                  method="post"
                                                  class="d-inline"
                                                  onsubmit="return confirm('Are you sure you want to delete this question?');">

                                                @Html.AntiForgeryToken()

                                                <button type="submit"
                                                        class="btn btn-sm btn-outline-danger"
                                                        title="Delete Question">

                                                    <i class="bi bi-trash"></i>

                                                </button>

                                            </form>

                                        </div>

                                    </td>

                                </tr>

                                questionNumber++;
                            }

                        </tbody>

                    </table>

                </div>
            }
            else
            {
                <!-- Empty Questions State -->

                <div class="text-center py-5">

                    <i class="bi bi-question-circle display-4 text-muted"></i>

                    <h5 class="mt-3">
                        No Questions Yet
                    </h5>

                    <p class="text-muted mb-4">
                        This quiz does not have any questions yet.
                    </p>

                    <a asp-area="Trainer"
                       asp-controller="Questions"
                       asp-action="Create"
                       asp-route-quizId="@Model.Id"
                       class="btn btn-primary">

                        <i class="bi bi-plus-lg me-1"></i>
                        Add First Question

                    </a>

                </div>
            }

        </div>

    </div>


    <!-- ================= QUIZ RESULTS ================= -->

    <div class="card shadow-sm border-0">

        <div class="card-header bg-white py-3">

            <h5 class="mb-1 fw-semibold">
                <i class="bi bi-bar-chart-line me-2"></i>
                Quiz Results
            </h5>

            <small class="text-muted">
                Results submitted by trainees.
            </small>

        </div>


        <div class="card-body p-0">

            @if (results.Any())
            {
                <div class="table-responsive">

                    <table class="table table-hover align-middle mb-0">

                        <thead class="table-light">

                            <tr>

                                <th class="ps-4">
                                    Trainee
                                </th>

                                <th>
                                    Score
                                </th>

                                <th>
                                    Status
                                </th>

                                <th>
                                    Submitted At
                                </th>

                            </tr>

                        </thead>

                        <tbody>

                            @foreach (var result in results)
                            {
                                <tr>

                                    <td class="ps-4">

                                        <div class="fw-semibold">
                                            @(result.User?.UserName ?? "Unknown")
                                        </div>

                                    </td>

                                    <td>

                                        <span class="fw-semibold">
                                            @result.Score
                                        </span>

                                    </td>

                                    <td>

                                        @if (result.IsPassed)
                                        {
                                            <span class="badge bg-success">
                                                Passed
                                            </span>
                                        }
                                        else
                                        {
                                            <span class="badge bg-danger">
                                                Failed
                                            </span>
                                        }

                                    </td>

                                    <td>

                                        <span class="text-muted">
                                            @result.SubmittedAt.ToString("dd MMM yyyy, hh:mm tt")
                                        </span>

                                    </td>

                                </tr>
                            }

                        </tbody>

                    </table>

                </div>
            }
            else
            {
                <!-- Empty Results State -->

                <div class="text-center py-5">

                    <i class="bi bi-bar-chart display-4 text-muted"></i>

                    <h5 class="mt-3">
                        No Results Yet
                    </h5>

                    <p class="text-muted mb-0">
                        No trainees have submitted this quiz yet.
                    </p>

                </div>
            }

        </div>

    </div>

</div>