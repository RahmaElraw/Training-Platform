@model Training_Platform.ViewModels.UserWithRelatedVM

@{
    ViewData["Title"] = Localizer["Users"];
}

<div class="card">

    <div class="card-header d-flex justify-content-between align-items-center">

        <h5 class="mb-0">
            @Localizer["UsersManagement"]
        </h5>

        <a asp-action="Create" class="btn btn-primary">
            <i class="fas fa-plus me-1"></i>
            @Localizer["AddUser"]
        </a>

    </div>

    <div class="card-body">

        <form asp-action="Index" method="get" class="row mb-4">

            <div class="col-md-4">
                <input asp-for="Query"
                       name="query"
                       class="form-control"
                       placeholder="@Localizer["SearchByUsernameOrEmail"]" />
            </div>

            <div class="col-md-2">
                <button class="btn btn-primary">
                    @Localizer["Search"]
                </button>
            </div>

        </form>

        <table class="table table-hover align-middle">

            <thead>

                <tr>

                    <th>@Localizer["UserName"]</th>
                    <th>@Localizer["Email"]</th>
                    <th>@Localizer["Role"]</th>
                    <th>@Localizer["Approved"]</th>
                    <th>@Localizer["Created"]</th>
                    <th width="260">@Localizer["Actions"]</th>

                </tr>

            </thead>

            <tbody>

                @if (Model.Users.Any())
                {
                    @foreach (var user in Model.Users)
                    {
                        <tr>

                            <td>@user.User.UserName</td>

                            <td>@user.User.Email</td>

                            <td>
                                @user.Role
                            </td>

                            <td>
                                @if (user.User.IsApproved)
                                {
                                    <span class="badge bg-success">
                                        @Localizer["Approved"]
                                    </span>
                                }
                                else
                                {
                                    <span class="badge bg-danger">
                                        @Localizer["Pending"]
                                    </span>
                                }
                            </td>

                            <td>@user.User.CreatedAt.ToShortDateString()</td>

                            <td>

                                <a asp-action="Details"
                                   asp-route-id="@user.User.Id"
                                   class="btn btn-info btn-sm">
                                    @Localizer["Details"]
                                </a>

                                <a asp-action="Edit"
                                   asp-route-id="@user.User.Id"
                                   class="btn btn-warning btn-sm">
                                    @Localizer["Edit"]
                                </a>

                                <br />

                                <form asp-action="ToggleStatus"
                                      asp-route-id="@user.User.Id"
                                      method="post"
                                      class="d-inline">

                                    @Html.AntiForgeryToken()

                                    @if (user.User.IsApproved)
                                    {
                                        <button type="submit"
                                                class="btn btn-danger btn-sm"
                                                onclick="return confirm('Are you sure you want to deactivate this user?');">
                                            <i class="fas fa-user-slash"></i>
                                            @Localizer["MakeDeactivate"]
                                        </button>
                                    }
                                    else
                                    {
                                        <button type="submit"
                                                class="btn btn-success btn-sm"
                                                onclick="return confirm('Are you sure you want to activate this user?');">
                                            <i class="fas fa-user-check"></i>
                                            @Localizer["MakeActivate"]
                                        </button>
                                    }

                                </form>

                            </td>

                        </tr>
                    }
                }
                else
                {
                    <tr>
                        <td colspan="5" class="text-center">
                            @Localizer["NoUsersFound"]
                        </td>
                    </tr>
                }

            </tbody>

        </table>

    </div>

</div>

@if (Model.TotalPages > 1)
{
    <nav class="mt-3">

        <ul class="pagination justify-content-center">

            @for (int i = 1; i <= Model.TotalPages; i++)
            {
                <li class="page-item @(i == Model.CurrentPage ? "active" : "")">

                    <a class="page-link"
                       asp-action="Index"
                       asp-route-page="@i"
                       asp-route-query="@Model.Query">

                        @i

                    </a>

                </li>
            }

        </ul>

    </nav>
}
